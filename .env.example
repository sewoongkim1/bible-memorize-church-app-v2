# Supabase 연결 값 (실제 값은 .env 에 넣고, .env 는 커밋하지 않음)
# 클라이언트(PWA)에서 사용 — 공개돼도 되는 값
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=<anon-public-key>

# 아래는 절대 클라이언트/저장소에 두지 말 것! Edge Function 의 시크릿으로만 설정
# SUPABASE_SERVICE_ROLE_KEY=<service-role-key>
# ADMIN_SECRET=<관리자 통계 접근용 비밀>
